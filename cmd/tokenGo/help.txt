tokenGo is a collection of commands to generate token.

Usage:

	tokenGo <command> [arguments]

The commands are:

	password	generate a password string

Use "tokenGo help <command>" for more information about a command.
